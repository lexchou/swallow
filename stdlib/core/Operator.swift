infix operator < {
    associativity none
    precedence 130
}

infix operator += {
    associativity right
    precedence 90
    assignment
}

infix operator ... {
    associativity none
    precedence 135
}

infix operator -= {
    associativity right
    precedence 90
    assignment
}

infix operator &% {
    associativity left
    precedence 150
}

infix operator && {
    associativity left
    precedence 120
}

infix operator /= {
    associativity right
    precedence 90
    assignment
}

infix operator &* {
    associativity left
    precedence 150
}

infix operator &+ {
    associativity left
    precedence 140
}

infix operator &- {
    associativity left
    precedence 140
}

infix operator &/ {
    associativity left
    precedence 150
}

infix operator === {
    associativity none
    precedence 130
}

infix operator ..< {
    associativity none
    precedence 135
}

infix operator == {
    associativity none
    precedence 130
}

infix operator ^= {
    associativity right
    precedence 90
    assignment
}

infix operator ?? {
    associativity right
    precedence 110
}

infix operator ^ {
    associativity left
    precedence 140
}

infix operator >> {
    associativity none
    precedence 160
}

infix operator &= {
    associativity right
    precedence 90
    assignment
}

infix operator % {
    associativity left
    precedence 150
}

infix operator & {
    associativity left
    precedence 150
}

infix operator *= {
    associativity right
    precedence 90
    assignment
}

infix operator * {
    associativity left
    precedence 150
}

infix operator | {
    associativity left
    precedence 140
}

infix operator + {
    associativity left
    precedence 140
}

infix operator ~> {
    associativity left
    precedence 255
}

infix operator - {
    associativity left
    precedence 140
}

infix operator / {
    associativity left
    precedence 150
}

infix operator <<= {
    associativity right
    precedence 90
    assignment
}

infix operator >= {
    associativity none
    precedence 130
}

infix operator !== {
    associativity none
    precedence 130
}

infix operator << {
    associativity none
    precedence 160
}

infix operator || {
    associativity left
    precedence 110
}

infix operator |= {
    associativity right
    precedence 90
    assignment
}

infix operator <= {
    associativity none
    precedence 130
}

infix operator ~= {
    associativity none
    precedence 130
}

infix operator %= {
    associativity right
    precedence 90
    assignment
}

infix operator != {
    associativity none
    precedence 130
}

infix operator > {
    associativity none
    precedence 130
}

infix operator >>= {
    associativity right
    precedence 90
    assignment
}

prefix operator - {
}

prefix operator ~ {
}

prefix operator -- {
}

prefix operator ++ {
}

prefix operator + {
}

prefix operator ! {
}

postfix operator ++ {
}

postfix operator -- {
}
